{
  "name": "btt-node",
  "version": "0.0.4",
  "description": "A tool for managing BTT webserver easier, in nodejs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "auto-changelog -p && git add CHANGELOG.md"
  },
  "auto-changelog": {
    "output": "CHANGELOG.md",
    "template": "keepachangelog",
    "unreleased": false,
    "commitLimit": false,
    "issueUrl": "https://github.com/Worie/btt-node/issues/{id}"
  },
  "author": "Wojciech Połowniak",
  "license": "MIT",
  "dependencies": {
    "node-fetch-polyfill": "^2.0.6"
  },
  "devDependencies": {
    "auto-changelog": "^1.7.0",
    "generate-changelog": "^1.7.1"
  }
}
